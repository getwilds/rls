% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/with.R
\name{with_db_user}
\alias{with_db_user}
\title{With database user or role}
\usage{
with_db_user(con, user, code)
}
\arguments{
\item{con}{(PqConnection) a PostgreSQL DBI connection from
\code{\link[RPostgres:Postgres]{RPostgres::Postgres()}}}

\item{user}{(character) a user or role to switch to to execute
the code in \code{code}}

\item{code}{a code block}
}
\description{
With database user or role
}
\section{How it works}{

Function temporarily assumes role or user as defined in
the \code{user} parameter, executes the code in \code{code}, then goes
back to the user/role outside of scope of the function
}

\section{Connections}{

Currently this function does not close the connection
passed to the function, but maybe we should? kinda like
\code{withr::with_db_connection} does?
}

\examples{
\dontshow{if (interactive() && has_postgres()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
library(DBI)
library(RPostgres)
con <- dbConnect(Postgres())

current1 <- rls_current_user(con)
current1

dbCreateTable(con, "cars", mtcars)
rows_append(
  tbl(con, "cars"),
  copy_inline(con, mtcars),
  in_place = TRUE
)

dbExecute(con, "CREATE ROLE analysts")
dbExecute(con, "GRANT SELECT ON TABLE cars TO analysts")

with_db_user(
 con = con,
 user = "analysts",
 code = dbGetQuery(con, "select * from cars")
)
# current user should be the same as above
current2 <- rls_current_user(con)
identical(current1, current2)

# cleanup
dbRemoveTable(con, "cars")
dbExecute(con, "DROP ROLE analysts")
dbDisconnect(con)
\dontshow{\}) # examplesIf}
}
