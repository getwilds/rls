% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/policies.R
\name{rls_privileges}
\alias{rls_privileges}
\title{Overview of privileges and policies}
\usage{
rls_privileges(con, table, user_role = NULL, schema = "public")
}
\arguments{
\item{con}{a postgres or redshift connection object}

\item{table}{(character) a table name}

\item{user_role}{(character) a user or role name. default: \code{NULL}.
if \code{NULL}, all users/roles returned}

\item{schema}{(character) a schema}
}
\description{
Overview of privileges and policies
}
\details{
\emph{Privileges} are broken down into two categories:
\itemize{
\item \strong{Table}: Some privileges can only be thought about at the table level,
e.g., truncate can only be applied to an entire table, not a column
\item \strong{Column}: Column level priveleges specify access to
}

Then there's \strong{Row} level \emph{policies}, which as the name says apply to
specific rows only

Both table and column level privileges use the SQL command \code{GRANT},
while row level policies use a separate command \verb{CREATE POLICY}.
Row level policies have specific names to them - whereas table and
column level privileges do not have names.
}
\section{Running examples}{

First run the code in \link{passwd}, then run the below code
}

\examples{
\dontshow{if (interactive() && has_postgres()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
library(RPostgres)
con <- dbConnect(Postgres())
rls_privileges(con, "passwd")
rls_privileges(con, "passwd", "aliceuser")

policy1 <- rls_construct_policy(
  name = "stuff",
  table = "passwd",
  using = "(true)"
)
rls_create_policy(con, policy1)
rls_privileges(con, "passwd")
\dontshow{\}) # examplesIf}
}
