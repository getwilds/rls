% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/row_policy.R
\name{translate_row_policy}
\alias{translate_row_policy}
\title{Translate row policy}
\usage{
translate_row_policy(policy, con)
}
\arguments{
\item{policy}{an S3 object of class \code{row_policy}, required}

\item{con}{DBI connection object, required}
}
\description{
Translate row policy
}
\examples{
\dontshow{if (interactive()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
library(RPostgres)
library(DBI)
con <- dbConnect(Postgres())
setup_example_table(con)

if (rls_policy_exists(con, "blue_policy")) {
  rls_drop_policy(con, name = "blue_policy", table = "passwd")
}

policy <- rls_tbl(con, "passwd") \%>\%
  row_policy(name = "blue_policy") \%>\%
  commands(update) \%>\%
  rows_existing(TRUE) \%>\%
  rows_new(TRUE) \%>\%
  to(jane)
policy
sql <- translate_row_policy(policy, con)
sql
dbExecute(con, sql)
\dontshow{\}) # examplesIf}
}
\keyword{internal}
