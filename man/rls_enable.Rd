% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enable.R
\name{rls_enable}
\alias{rls_enable}
\alias{rls_disable}
\title{Enable row level security on a table}
\usage{
rls_enable(con, table)

rls_disable(con, table)
}
\arguments{
\item{con}{a DBI database connection object. required. supports only
postgres and redshift connections}

\item{table}{(character) a table name. required}
}
\value{
a scalar numeric that specifies the number of rows affected
by the statement, invisibly
}
\description{
Enable row level security on a table
}
\examples{
\dontshow{if (has_postgres()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
library(DBI)
library(RPostgres)
con <- dbConnect(Postgres())
dbListTables(con)
dbWriteTable(con, "mtcars", mtcars, temporary = TRUE)
rls_enable(con, table = "mtcars")
rls_check_status(con, "mtcars")
rls_disable(con, table = "mtcars")
rls_check_status(con, "mtcars")
dbRemoveTable(con, "mtcars")
dbDisconnect(con)
\dontshow{\}) # examplesIf}
}
