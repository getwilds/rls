% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exists.R
\name{rls_policy_exists}
\alias{rls_policy_exists}
\title{Check if a row level security policy exists}
\usage{
rls_policy_exists(con, name)
}
\arguments{
\item{con}{a DBI database connection object. required. supports only
postgres and redshift connections}

\item{name}{(character) a policy name. required}
}
\value{
scalar boolean, \code{TRUE} or \code{FALSE}
}
\description{
Check if a row level security policy exists
}
\examples{
\dontshow{if (has_postgres()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
library(DBI)
library(RPostgres)
con <- dbConnect(Postgres())
dbWriteTable(con, "attitude", attitude)
policy <- rls_tbl(con, "attitude") \%>\%
  row_policy(name = "some_policy") \%>\%
  commands(update) \%>\% 
  rows_existing(TRUE) \%>\%
  to(jane)
policy
rls_run(policy)
rls_policies(con)
rls_policy_exists(con, "some_policy")
rls_drop_policy(con, name = "some_policy", table = "attitude")
rls_policy_exists(con, "some_policy")
dbRemoveTable(con, "attitude")
dbDisconnect(con)
\dontshow{\}) # examplesIf}
}
